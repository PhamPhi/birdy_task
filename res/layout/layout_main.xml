<?xml version="1.0" encoding="utf-8"?>

<android.support.v4.widget.DrawerLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/drawer_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <!-- Putting the main frame layout at here... -->
    <FrameLayout android:id="@+id/frame_main_container"
                 android:layout_width="match_parent"
                 android:layout_height="match_parent"
                 android:background="@drawable/birdy_task_background">

    </FrameLayout>

    <ListView android:id="@+id/menu_slider_main"
              android:layout_width="@dimen/list_slider_menu_width"
              android:layout_height="match_parent"
              android:layout_gravity="start"
              android:choiceMode="singleChoice"
              android:divider="@drawable/list_divider"
              android:dividerHeight="1dp"
              android:listSelector="@drawable/slider_menu_selector"
              android:scrollbars="none"
              android:background="@drawable/menu_drawer_background"/>

</android.support.v4.widget.DrawerLayout>